import { StyleSheet } from "react-native"

const LearnStyles = StyleSheet.create({
  safe:{
    flex:1,
  },
  top:{
    flexDirection:'row',
  },
  topic:{
      fontSize:25,
      fontWeight:'bold',
      marginTop:10,  }
})

export default LearnStyles